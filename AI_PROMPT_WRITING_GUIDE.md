# AI 提示词编写指南
**专注于代码审查、架构分析和代码生成**

---

## 🎯 核心原则

优秀的 AI 提示词遵循 **SMART** 原则：
- **S** (Specific): 具体明确，避免模糊表述
- **M** (Measurable): 可衡量结果
- **A** (Actionable): 可执行，AI 能实施的操作
- **R** (Relevant): 与当前任务相关
- **T** (Targeted): 针对特定代码或问题

---

## 📝 提示词的 5 大核心要素

### 1️⃣ **任务定义** (WHAT)
明确告诉 AI 要做什么

```markdown
❌ 不好的表述：
"审查这个代码"

✅ 好的表述：
"对以下 C 代码进行静态代码审查，重点检查：
1. 内存泄漏风险
2. 数组越界可能
3. 空指针解引用
4. 整型溢出"
```

### 2️⃣ **上下文信息** (WHY & WHERE)
提供充分的背景信息

```markdown
❌ 缺乏上下文：
"这个函数有问题吗？"

✅ 充分的上下文：
"这是 DM9051 网络驱动的 SPI 初始化函数。
当前代码运行在 MH2030A MCU 上，
使用 STM32 标准库（不是 HAL 库）。
系统支持三种模式：轮询、DMA、中断。
环境: Keil uVision, ARM Cortex-M0+, 64KB RAM"
```

### 3️⃣ **具体范围** (SCOPE)
指定代码文件、函数或行数范围

```markdown
❌ 范围模糊：
"看看整个项目"

✅ 明确的范围：
"分析以下文件：
1. drivers/dm9051_edriver_v1.6.1a_beta/core/dm9051.c (第 1-200 行)
2. port/mh2030a/mh2030a_dm9051_spi.c (全文)
3. port/uip/dm9051_uip_adapter.c (关注 poll 函数)"
```

### 4️⃣ **预期输出格式** (FORMAT)
指定你希望得到什么格式的结果

```markdown
❌ 无格式要求：
"给我建议"

✅ 明确的格式：
"输出格式 (Markdown):
## 问题摘要
- [x] 检查项1
- [x] 检查项2

## 发现的问题
### 问题1: {标题}
**严重程度**: HIGH | MEDIUM | LOW
**位置**: {文件}:{行号}
**描述**: {问题描述}
**修复方案**: {代码片段}

## 总体评分
- 代码质量: 6/10
- 可维护性: 5/10"
```

### 5️⃣ **成功标准** (ACCEPTANCE)
定义什么算是好的输出

```markdown
❌ 含糊的标准：
"给我个好答案"

✅ 具体的标准：
"好的输出应该：
1. 标识至少 5 个具体的代码问题
2. 每个问题都有行号引用
3. 每个问题都给出改进前后的代码示例
4. 总结时指出 3 个最关键的优化方向"
```

---

## 🔧 代码审查类提示词模板

### 模板 1: 代码质量审查

```markdown
# 代码质量审查请求

## 任务
对以下 C 代码进行全面的代码质量审查。

## 上下文
- **项目**: MH2030 DM9051 网络驱动
- **用途**: 硅传网卡的 HAL 驱动层
- **平台**: MH2030A MCU (ARM Cortex-M0+)
- **约束**: 支持 3 种 SPI 模式（轮询/DMA/中断）
- **关键指标**: 系统内存 64KB，需要运行 uIP 网络栈

## 审查范围
**文件列表:**
1. `drivers/dm9051_edriver_v1.6.1a_beta/include/dm9051_hal_api.h` - HAL 接口定义
2. `port/mh2030a/mh2030a_dm9051_spi.c` - 轮询模式实现
3. `port/mh2030a/mh2030a_dm9051_spi_dma.c` - DMA 模式实现
4. `port/mh2030a/mh2030a_dm9051_int.c` - 中断模式实现

## 审查重点
1. **内存安全性**
   - 缓冲区边界检查
   - 空指针检查
   - 栈溢出风险
   
2. **代码质量**
   - 命名规范一致性
   - 函数复杂度评估
   - 魔数识别和命名
   
3. **性能**
   - 中断响应时间
   - SPI 吞吐量优化机会
   - 内存布局优化
   
4. **可维护性**
   - 代码重复度
   - 硬件配置耦合度
   - 网络栈解耦程度

## 输出格式
使用以下 Markdown 格式：

```
## 代码审查报告

### 1. 问题列表
| ID | 文件 | 行号 | 严重程度 | 问题类型 | 描述 |
|----|------|------|---------|---------|------|
| P1 | mh2030a_dm9051_spi.c | 45 | HIGH | 内存 | 缓冲区越界 |

### 2. 详细分析

#### P1: SPI 传输缓冲区越界
**位置**: mh2030a_dm9051_spi.c:45-52
**严重程度**: 🔴 HIGH
**类型**: 内存安全性

**问题代码**:
\`\`\`c
// 存在问题的代码
\`\`\`

**问题说明**: ...

**改进方案**:
\`\`\`c
// 改进后的代码
\`\`\`

### 3. 总体评估
- 总问题数: X 个 (HIGH: X, MEDIUM: X, LOW: X)
- 代码质量评分: 6/10
- 最关键的 3 个改进方向:
  1. ...
  2. ...
  3. ...
```

## 成功标准
✅ 输出应该包含：
- [ ] 至少 5 个具体的代码问题，每个都有行号
- [ ] 每个 HIGH/MEDIUM 问题都有改进前后的代码对比
- [ ] 性能分析（如 SPI 超时处理的影响）
- [ ] 可维护性建议（代码重复、配置耦合）
- [ ] 优先级排序
```

### 模板 2: 代码重复分析

```markdown
# 代码重复分析请求

## 任务
识别项目中重复的代码段，并提出统一方案。

## 涉及文件
- port/mh2030a/mh2030a_dm9051_spi.c (轮询模式)
- port/mh2030a/mh2030a_dm9051_spi_dma.c (DMA 模式)
- port/mh2030a/mh2030a_dm9051_int.c (中断模式)

## 分析维度
1. **GPIO 初始化** - 这三个文件都需要初始化 CS、SCK、MOSI、MISO、RST
2. **SPI 参数配置** - 波特率、极性、相位设置
3. **硬件超时处理** - 都有 SPI 超时检查
4. **中断控制** - 都需要开启/关闭中断

## 输出要求
对每组重复代码，输出：
1. **重复代码块** (原始位置)
2. **通用化设计** (提取的公共函数)
3. **变化部分** (如何参数化)
4. **迁移步骤** (具体改动)

格式示例：
\`\`\`
### 重复块 1: GPIO 初始化
**发现位置**: 3 处 (spi.c, dma.c, int.c)

**提议的公共函数**:
\`\`\`c
void hal_spi_configure_gpio(const hal_spi_pin_config_t *pins);
\`\`\`
\`\`\`
```

---

## 🏗️ 架构分析类提示词

### 模板 3: 架构设计评审

```markdown
# 架构设计评审请求

## 任务
评审当前的三层架构设计，识别耦合问题和改进机会。

## 当前架构

应用层 (main.c) 
  ↓
uIP 适配层 (dm9051_uip_adapter.c)
  ↓
DM9051 核心驱动 (dm9051.c)
  ↓
HAL 层 (hal_spi_*.c)
  ↓
MCU 驱动库

## 关键问题
1. **网络栈耦合**: 如何在不修改 DM9051 驱动的情况下支持 lwIP?
2. **配置分散**: 硬件引脚配置硬编码在各个 .c 文件中
3. **模式选择**: 三种 SPI 模式通过编译时条件选择，不够灵活

## 审查要点
1. **分层清晰度** - 各层职责是否明确?
2. **接口设计** - 接口是否足够稳定?
3. **扩展性** - 添加新网络栈需要改多少代码?
4. **可测试性** - 各层是否能独立测试?
5. **性能** - 分层是否导致性能问题?

## 输出格式
### 架构评估报告

#### 当前架构的优点
- ...

#### 存在的问题
1. **问题**: ...
   **影响**: ...
   **风险**: ...

#### 改进建议
1. **建议**: ...
   **实现**: ...
   **收益**: ...

#### 优化架构图
\`\`\`
新的分层设计...
\`\`\`

#### 迁移策略
- Phase 1: ...
- Phase 2: ...
```

### 模板 4: 为新功能设计扩展点

```markdown
# 新功能扩展点设计

## 任务
为项目设计扩展点，使其能够：
1. 支持 lwIP 网络栈（未来）
2. 支持其他硬件平台（未来）
3. 支持多个 DM9051 实例（未来）

## 当前限制
1. 网络栈写死为 uIP
2. 硬件配置硬编码
3. 全局变量导致单实例限制

## 设计要求
1. **向后兼容** - 不能破坏现有代码
2. **最小改动** - 现有 uIP 使用代码不需要修改
3. **清晰接口** - 新网络栈集成应该简单
4. **文档完善** - 提供集成指南

## 输出
1. **通用网络驱动接口** (结构体设计)
2. **uIP 适配器迁移步骤**
3. **lwIP 适配器框架代码**
4. **多实例支持方案**
5. **集成指南文档**
```

---

## 💻 代码生成类提示词

### 模板 5: 提取公共代码

```markdown
# 提取公共代码请求

## 任务
从三个 SPI 实现文件中提取公共的初始化和配置代码。

## 源文件
- port/mh2030a/mh2030a_dm9051_spi.c (轮询)
- port/mh2030a/mh2030a_dm9051_spi_dma.c (DMA)
- port/mh2030a/mh2030a_dm9051_int.c (中断)

## 要提取的部分
1. **GPIO 初始化** - CS, SCK, MOSI, MISO, RST 引脚配置
2. **SPI 参数设置** - 时钟、模式、数据宽度
3. **硬件复位** - RST 引脚时序
4. **超时处理** - 通用的超时计数逻辑
5. **中断控制** - 使能/禁止 MCU 中断

## 输出要求
生成以下文件框架：

1. **hal_spi_common.h** - 公共接口声明
2. **hal_spi_common.c** - 公共实现
3. **hal_mh2030a_config.h** - 配置集中地
4. **修改说明** - 如何重构 3 个 .c 文件

## 约束条件
- 保持现有的函数签名 (hal_read_reg, hal_write_reg 等)
- 不改变编译时模式选择机制 (保持现有 Keil 项目配置)
- 完整的文档注释
- 代码风格与现有代码保持一致
```

### 模板 6: 实现新的适配器

```markdown
# lwIP 网络驱动适配器代码生成

## 任务
基于现有 uIP 适配器的设计模式，为 lwIP 生成完整的适配器代码框架。

## 参考实现
参考文件: `port/uip/dm9051_uip_adapter.c`

主要函数:
- dm9051_netif_open() - 初始化
- dm9051_netif_input() - 接收数据
- dm9051_netif_output() - 发送数据
- dm9051_uip_adapter_poll() - 轮询处理

## 目标文件
生成以下 lwIP 适配文件：
1. port/lwip/dm9051_lwip_adapter.h
2. port/lwip/dm9051_lwip_adapter.c
3. port/lwip/netconf_mh2030a_lwip.h
4. port/lwip/netconf_mh2030a_lwip.c
5. port/lwip/ethernetif.c (lwIP 标准接口)

## 关键要求
1. **使用相同的 dm9051_netif.h 接口** - 核心驱动不需要改变
2. **支持 lwIP 的中断模式** - 异步接收处理
3. **缓冲区管理** - 适配 lwIP 的 pbuf 机制
4. **错误处理** - 与 uIP 版本保持一致
5. **构建配置** - 通过 #define 条件编译选择 uIP 或 lwIP

## 代码质量要求
- Doxygen 文档注释完整
- 错误处理全面
- 内存泄漏检查
- 与现有代码风格一致
```

---

## 🎨 提示词优化技巧

### 技巧 1: 使用结构化问题描述

```markdown
❌ 不清楚：
"这个代码是否有问题？"

✅ 清晰：
"分析这段代码是否存在以下问题：
1. 当 len == 0 时，hal_read_mem(buf, 0) 的行为
2. 缓冲区 buf 在调用前是否被验证非 NULL
3. 当 SPI 超时时的错误传播"
```

### 技巧 2: 提供对比示例

```markdown
❌ 抽象：
"改进代码质量"

✅ 具体：
"当前实现存在的问题:
\`\`\`c
uint8_t hal_read_reg(uint8_t reg) {
    return transfer_spi_byte(reg);
}
\`\`\`

改进方向 (参考):
\`\`\`c
hal_status_t hal_read_reg(uint8_t reg, uint8_t *val) {
    if (val == NULL) return HAL_ERR_PARAM;
    *val = transfer_spi_byte(reg);
    return HAL_OK;
}
\`\`\`"
```

### 技巧 3: 分阶段提问

```markdown
❌ 一次性提问:
"优化这个项目"

✅ 分阶段:
"第一步：分析现有的 SPI 初始化代码，找出公共部分。
输出：
1. 三个文件中的相同代码片段列表
2. 参数化方案

第二步：(基于第一步结果) 设计统一的初始化函数..."
```

### 技巧 4: 指定参考资料

```markdown
"参考这些 Linux 内核中的设计模式：
- 参考 1: drivers/net/ethernet/davicom/dm9051.c
- 参考 2: NETDEV(net_device_ops) 虚函数表
- 参考 3: struct sk_buff 的缓冲区管理

请应用类似的模式到我们的代码中..."
```

---

## ❌ 常见错误及改正

| 错误 | 问题 | 正确做法 |
|------|------|---------|
| "审查这个代码" | 太模糊 | 指定具体文件、行号、审查重点 |
| "帮我优化" | 没有目标 | 明确优化目标：性能/可维护性/可扩展性 |
| "生成 lwIP 驱动" | 缺少上下文 | 提供 uIP 驱动作为参考，说明差异点 |
| "这样对吗？" | 不可衡量 | 定义验收标准 |
| 一次性大任务 | 容易出错 | 分阶段提问 |

---

## 📋 提示词检查清单

在提交提示词前，检查：

- [ ] **明确的任务** - AI 知道要做什么吗?
- [ ] **充分的上下文** - AI 理解背景信息吗?
- [ ] **具体的范围** - AI 知道范围限制吗?
- [ ] **清晰的格式** - AI 知道你期望什么形式的输出?
- [ ] **可衡量的标准** - 什么样的输出算是成功?
- [ ] **文件路径** - 是否提供了完整、正确的文件路径?
- [ ] **代码示例** - 是否提供了当前代码作为参考?
- [ ] **约束条件** - 是否说明了限制条件(向后兼容、性能约束等)?

---

## 🚀 高级技巧

### 使用 Role-Play 增强专注度

```markdown
"你是一名拥有 20 年 C 嵌入式系统开发经验的资深工程师，
专长在驱动程序设计和 HAL 架构。

基于这个背景，审查以下代码并提出建议..."
```

### 使用链式思考 (Chain-of-Thought)

```markdown
"请按以下步骤分析：

第 1 步: 理解当前架构
- 画出数据流图
- 列出关键接口

第 2 步: 识别问题
- 代码重复在哪?
- 耦合点在哪?

第 3 步: 提出方案
- 如何解耦?
- 如何复用?

第 4 步: 评估风险
- 向后兼容吗?
- 性能影响?"
```

### 使用分析框架

```markdown
# 使用 RACI 矩阵分析代码责任

对于三个 SPI 实现 (spi.c, dma.c, int.c)，
分析谁应该 (R)esponsible, (A)ccountable, (C)onsulted, (I)nformed？

这可以帮助理解代码所有权和架构设计..."
```

---

## 📚 最佳实践总结

### 🎯 目标清晰
> "我需要识别 DM9051 驱动中的代码重复，以便抽取公共层"

### 📍 范围明确
> "分析这 3 个文件，重点关注 SPI 初始化和 GPIO 配置部分"

### 🔍 标准量化
> "输出应该包含：具体的代码块位置、提议的抽取方案、迁移步骤"

### 📊 格式规定
> "使用 Markdown 表格列出重复代码，每个条目包含 [位置, 行数, 重复度%]"

### ✅ 验证标准
> "好的分析应该让我能直接改代码，而不需要再问问题"

---

## 💡 本项目的推荐提示词

对于 **mh2030_demo 项目**，推荐的高效提示词组合：

### 第一阶段: 全景分析
```markdown
分析 d:\prg\davicom\mh2xxxx_proj\mh2030_demo 项目的 DM9051 驱动实现。

重点:
1. 当前的分层架构 (HAL -> 核心驱动 -> 网络栈 -> 应用)
2. 三种 SPI 模式 (轮询/DMA/中断) 的代码重复度
3. 网络栈与硬件的耦合程度
4. 为 lwIP 支持所需的改动

输出:
- 架构图 (ASCII or Mermaid)
- 代码质量评分 (各维度)
- TOP 5 优化机会 (按优先级)
```

### 第二阶段: 深度改进
```markdown
基于之前的分析，为"提取 SPI 公共代码"任务生成:

1. hal_spi_common.h 框架
2. hal_mh2030a_config.h 框架
3. 三个文件 (spi.c/dma.c/int.c) 的重构步骤
4. 迁移检查清单

约束: 保持 Keil 现有项目兼容性
```

---

## 📞 需要帮助?

如果不确定如何表述，使用这个模板：

```markdown
**任务**: [你想让 AI 做什么]
**为什么**: [原因是什么]
**文件**: [具体是哪些文件]
**现状**: [当前代码的样子]
**期望**: [你想要的结果]
**约束**: [有什么限制条件]
```
